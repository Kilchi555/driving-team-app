# Secrets / Config Check — 2026-09-04

**Scope:** Hardcoded keys in app code, `.env` in diffs/PRs, missing Internal-Secret headers on server→server calls  
**Baseline:** `origin/main` @ `571e2e8f`  
**Result:** Keine neuen Findings

## Checks performed

1. **Keys in code** — Pattern scan for Stripe (`sk_`/`rk_`/`whsec_`), Supabase (`sb_secret_`, service-role JWTs), AWS `AKIA`, GitHub `ghp_`, PEM private keys in tracked non-example sources. No live secret values found. Truncated JWT samples in guide docs and placeholder examples remain out of scope. Diff since last scan (`2bba3b99`→`571e2e8f`) introduced no literal secret-like strings.
2. **`.env` in diffs** — No non-example `.env` tracked in git. Open feature/deps PRs checked (#148, #146, #145, #143, #141, #133, #131, #128, #126, #125, #108, #90, #81, #73): no `.env` additions and no embedded live keys in diffs. Only `apps/simy/.env.example` and `apps/website/.env.example` are tracked.
3. **Internal-Secrets on server calls** — Callers of protected endpoints (`affiliate/process-reward`, `email/send-withdrawal-notification`, `admin/notify-new-tenant`, `appointments/handle-cancellation`, `email/send-wallee-payment-link`, `email/send-appointment-notification`, `reminders/send-payment-confirmation`, `wallee/save-payment-token`, `emails/send-booking-proposal`, website→simy `upload-inquiry-conversion`) all pass `x-internal-secret` / `X-Internal-Api-Secret` or `internalSecretHeaders()`. No missing-header call sites found.

## Findings

Keine neuen Findings.
