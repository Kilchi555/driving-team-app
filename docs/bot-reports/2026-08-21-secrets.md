# Secrets / Config Check — 2026-08-21

**Scope:** Hardcoded keys in app code, `.env` in diffs/PRs, missing Internal-Secret headers on server→server calls  
**Baseline:** `origin/main` @ `8b44092a`  
**Result:** Keine neuen Findings

## Checks performed

1. **Keys in code** — Pattern scan for Stripe (`sk_`/`rk_`/`whsec_`), Supabase (`sb_secret_`, service-role JWTs), AWS `AKIA`, GitHub `ghp_`, PEM private keys in tracked non-example sources. No live secret values found. Documented public client keys (Firebase `GoogleService-Info.plist`, anon/Maps examples in analysis docs) are out of scope.
2. **`.env` in diffs** — No non-example `.env` tracked in git. Last 30 days / open feature PRs (#53, #48, #13, #5, #4, #3, #2): no `.env` additions.
3. **Internal-Secrets on server calls** — Callers of protected endpoints (`affiliate/process-reward`, `email/send-withdrawal-notification`, `admin/notify-new-tenant`, `appointments/handle-cancellation`, `email/send-wallee-payment-link`, `email/send-appointment-notification`, `reminders/send-payment-confirmation`, website→simy `upload-inquiry-conversion`) all pass `x-internal-secret` / `X-Internal-Api-Secret` or `internalSecretHeaders()`.

## Findings

Keine neuen Findings.
