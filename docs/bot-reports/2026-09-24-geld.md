# Geld-Sicherheitsbericht — 2026-09-24

Scope: Zahlung, Guthaben, Auszahlung, Wallee/Stripe only. Confirmed medium/high/critical with clear attack stories on current tree (`origin/main` @ `e32c5acb185d153e217c2ff1b4a3e5e7c9e25262`). No app/DB code changes; report only. Read-only exploration.

**Verdict:** **6 of 7** previously open findings **RECONFIRMED**. Finding **6 remains FIXED** (`/api/credit/use-for-appointment` → atomic `applyCreditToPayment` / `apply_credit_to_payment` with freeze). **0 new** confirmed medium+ money bugs in the hunted hotspots beyond the open set. Main tip since 2026-09-17 audit; money-related commits since then (e.g. course-enroll auth `#239`, Wallee discount CHF fix) do **not** close findings 1–5 or 7.

---

## 1. Relevant surfaces (inventory)

### Wallee payment / webhook / sync
| Path | Role |
|------|------|
| `server/api/wallee/webhook.post.ts` | Live Wallee webhook; API verify + remaining-amount gate |
| `server/api/webhooks/wallee-payment-success.post.ts` | Legacy internal-only post-pay appointment create (`assertInternalSecret`) |
| `server/api/wallee/create-transaction.post.ts` | Create/reuse Wallee tx; server remaining amount for charge |
| `server/api/wallee/save-payment-token.post.ts` | Token save |
| `server/api/payments/process.post.ts` | Auth’d process + credit apply + Wallee start / existing-tx complete |
| `server/api/payments/process-public.post.ts` | Public course pay; server recomputes amount |
| `server/api/payments/update-wallee-id.post.ts` | Attach/overwrite `wallee_transaction_id` |
| `server/api/payments/status.post.ts`, `release-processing-lock.post.ts` | Client sync via `wallee-payment-sync` |
| `server/utils/wallee-payment-sync.ts` | State→DB sync (`mark_completed` without amount on non-course) |
| `server/utils/wallee-remaining-amount.ts` | Capture vs remaining payable (`shouldRejectWalleeCaptureMismatch`) |
| `server/utils/fulfill-course-wallee-payment.ts` | Course fulfill; amount-gated |
| `server/utils/topup-credit.ts` | Idempotent top-up deposit + `completeCapturedWalleePayment` |
| `server/api/cron/recover-pending-wallee-payments.get.ts` | Cron recover; course amount-gated, generic complete not |
| `server/api/courses/enroll-wallee.post.ts` | Course + Wallee enroll |
| `server/api/admin/wallee-*.ts` | Onboarding / test / activate (staff) |

### Guthaben / Auszahlung
| Path | Role |
|------|------|
| `server/api/customer/request-credit-withdrawal.post.ts` | Customer IBAN withdrawal request (freeze pending) |
| `server/api/customer/create-topup-session.post.ts` | Customer top-up session (credit via webhook) |
| `server/api/customer/get-withdrawal-status.get.ts`, `update-withdrawal-iban.post.ts` | Status / IBAN prefs |
| `server/api/admin/complete-withdrawal.post.ts` | Staff/admin marks bank payout done; clears pending + balance |
| `server/api/admin/pending-withdrawals.get.ts`, `export-withdrawal-payments.get.ts` | Admin lists/export |
| `server/api/student-credits/process-withdrawal-wallee.post.ts` | **410 disabled** |
| `server/api/student-credits/withdraw.post.ts`, `manual-voucher.post.ts` | Withdraw / voucher |
| `server/api/credit/use-for-appointment.post.ts` | Staff apply credit → `applyCreditToPayment` (**freeze-aware**) |
| `server/api/staff/credit-transaction.post.ts` | Staff mint/debit credit (role-gated) |
| `server/utils/wallet-atomic.ts` | `deductStudentCredit` / `addPendingWithdrawal` RPCs (**underused** on critical paths) |
| `server/utils/apply-credit-to-payment.ts` + `migrations/20260915_apply_credit_to_payment.sql` | Atomic freeze-aware apply |
| `migrations/20260826_wallet_atomic_ops.sql` | Atomic wallet ops (`deduct_student_credit`, `add_pending_withdrawal`) |

### Affiliate payout
| Path | Role |
|------|------|
| `server/api/affiliate/request-payout.post.ts` | Partner bank payout request + SMS OTP |
| `server/api/affiliate/confirm-payout.post.ts` | OTP confirm |
| `server/api/affiliate/admin-payouts.ts` | Admin payout ops |

### Stripe (SaaS subscription / Connect — not student Guthaben)
| Path | Role |
|------|------|
| `server/api/stripe/webhook.post.ts` | `constructEvent` signature verify |
| `server/api/stripe/create-checkout-session.post.ts` + portal / cancel / update / sync | Auth’d tenant billing |
| `server/api/stripe/connect/*` | Connect onboarding |

### Invoices / admin payment ops / shop
| Path | Role |
|------|------|
| `server/api/admin/payment-operations.post.ts` | Staff/admin payment mutations (role-gated) |
| `server/api/admin/payments/refund.post.ts` | Wallee refund; role + email allowlist |
| `server/api/invoices/*`, `server/api/shop/create-payment.post.ts` | Invoice / shop payment flows |
| `server/api/debug/*wallee*`, `*webhook*`, `*payment*` | Auth’d debug (super_admin for webhook-test) |

### RLS / ledger
| Path | Role |
|------|------|
| `migrations/fix_student_credits_staff_rls.sql` | Historical own UPDATE policy on `student_credits` |
| `migrations/fix_rls_security_audit_march2026.sql` | **Drops** customer `student_credits` self-UPDATE (KRITISCH-04) |
| `migrations/20260917_payments_jwt_postgrest_slice_a.sql` | Payments write via service_role only |
| `sql_migrations/20260620_wallee_space_id_multitenancy.sql` | Unique on `payment_wallee_transactions`, **not** `payments` |

---

## 2. Auth / authorization on money-moving paths

| Path | AuthZ |
|------|-------|
| `payments/process` | Authenticated user; payment scoped to tenant; owner/process lock |
| `payments/update-wallee-id` | Auth; owner or staff/admin; overwrite of *different* id blocked for non-staff |
| `payments/process-public` | Public + validation; amount recomputed from DB course price |
| `wallee/webhook` | No HMAC (Wallee limitation); verifies tx via Wallee API; amount mismatch reject |
| `webhooks/wallee-payment-success` | Internal secret + payment must already be paid |
| `wallee/create-transaction` | Auth; DB remaining amount authoritative for charge |
| `customer/request-credit-withdrawal` | Auth; own wallet; rate limit |
| `customer/create-topup-session` | Auth; min/max amount; credit only after Wallee capture |
| `admin/complete-withdrawal` | Auth; role `admin`\|`staff`; tenant-scoped |
| `affiliate/request-payout` | Auth; feature flag; SMS OTP before admin-visible |
| `credit/use-for-appointment` | Auth; staff/admin; RPC freeze-aware |
| `staff/credit-transaction` | Auth; staff/admin/super_admin/tenant_admin |
| `admin/payments/refund` | Auth; staff/admin + email allowlist |
| `stripe/webhook` | Stripe signature (`constructEvent`) |
| Stripe checkout/portal/etc. | Auth; tenant from caller |
| Cron recover | Cron/internal secret |
| Debug webhook-test | `super_admin` only |

---

## 3–4. Findings (re-check)

### 1. Critical — Wallee tx rebind → high-amount payment marked completed without amount/uniqueness check

**Status: CONFIRMED**

**Attack story:** Attacker (or same-tenant peer) obtains a low-amount completed Wallee `transactionId` `T`. They own a high-amount pending payment `P` with null `wallee_transaction_id`. `POST /api/payments/update-wallee-id` allows first-attach of any `T` with no Wallee read, amount check, merchantReference bind, or uniqueness. Then `POST /api/payments/process`, `create-transaction`, or client sync via `status` / `release-processing-lock` → `wallee-payment-sync` marks `P` completed on Wallee **state only** (FULFILL/COMPLETED/SUCCESSFUL). Webhook underpay (`shouldRejectWalleeCaptureMismatch`) is not used on these paths. `payments` still has no unique constraint on `(wallee_space_id, wallee_transaction_id)` (only `payment_wallee_transactions` is unique).

**Evidence:**
- `server/api/payments/update-wallee-id.post.ts:56–88` — overwrite of a *different* id blocked for non-staff; first-attach still writes arbitrary `wallee_transaction_id` with no amount/API verify.
- `server/api/payments/process.post.ts:576–595` — existing completed tx → `completeCapturedWalleePayment` (non-course) with no capture vs remaining compare; course path checks via fulfill helpers.
- `server/utils/wallee-payment-sync.ts:175–206` — `applyWalleeSyncDecision` `mark_completed` is state→DB only.
- `server/api/wallee/create-transaction.post.ts:132–173` — non-course completed reuse updates `payment_status: 'completed'` without amount gate.
- `server/api/cron/recover-pending-wallee-payments.get.ts:67–81` vs `:318` — amount mismatch enforced for **course** atomic fulfill only; generic `recoverToCapturedStatus` → `completeCapturedWalleePayment` does not.
- Contrast OK: `server/api/wallee/webhook.post.ts` (~604) + `server/utils/wallee-remaining-amount.ts:47` (`shouldRejectWalleeCaptureMismatch`).
- DB: `sql_migrations/20260620_wallee_space_id_multitenancy.sql` — unique only on `payment_wallee_transactions`, not `payments`.

---

### 2. Critical — Frozen withdrawal spendable via `/api/payments/process`

**Status: CONFIRMED**

**Attack story:** Student freezes CHF 100 via `request-credit-withdrawal` (`pending_withdrawal_rappen` rises; `balance_rappen` unchanged). They then pay a lesson through `/api/payments/process`. Process selects only `balance_rappen`, treats full balance as available, and RMW-updates balance (full or partial credit). Later admin `complete-withdrawal` still pays out / clears pending against the same wallet → double use of frozen funds.

**Evidence:**
- `server/api/payments/process.post.ts:174–186` — `select('balance_rappen')` only; `availableCredit = creditData?.balance_rappen`.
- `server/api/payments/process.post.ts:409` and `:524` — deduct via plain `.update({ balance_rappen: newBalance })`.
- Atomic helper unused here: `server/utils/wallet-atomic.ts:24` `deductStudentCredit` / `migrations/20260826_wallet_atomic_ops.sql` (`balance - pending >= amount`).

---

### 3. Critical — Affiliate `request-payout` ignores `pending_withdrawal_rappen`

**Status: CONFIRMED**

**Attack story:** Same freeze as above. Affiliate with `type: 'bank'` requests payout up to full `balance_rappen`. Check and deduct ignore pending → payout hold overlaps frozen IBAN withdrawal.

**Evidence:**
- `server/api/affiliate/request-payout.post.ts:59–73` — `select('balance_rappen')`; `currentBalance < amount_rappen`.
- `server/api/affiliate/request-payout.post.ts:91–96` — `update({ balance_rappen: newBalance })` with no pending subtraction and no row-level available guard.

---

### 4. High — Concurrent `request-credit-withdrawal` RMW (unused `add_pending_withdrawal`)

**Status: CONFIRMED**

**Attack story:** Two parallel requests each read available = balance − pending, both pass, both insert withdrawal rows and write `pending = stalePending + amount`. Without conditional update / RPC, frozen total can exceed spendable balance.

**Evidence:**
- `server/api/customer/request-credit-withdrawal.post.ts:49–58, 112` — read-modify-write on `pending_withdrawal_rappen`.
- Unused: `addPendingWithdrawal` in `server/utils/wallet-atomic.ts:64` + `add_pending_withdrawal` in `migrations/20260826_wallet_atomic_ops.sql`.

---

### 5. High — Affiliate bank payout concurrent race

**Status: CONFIRMED**

**Attack story:** Two parallel `request-payout` bank calls both read the same `balance_rappen`, both pass the sufficiency check, both write `balance - amount`. Wallet can go negative or double-hold the same funds into two `affiliate_payout_requests`.

**Evidence:**
- `server/api/affiliate/request-payout.post.ts:59–96` — non-atomic read then unconditional update; no `deduct_student_credit` / compare-and-swap.

---

### 6. High — `/api/credit/use-for-appointment` spends frozen balance

**Status: FIXED** (unchanged since 2026-09-17)

**Attack story (closed):** Previously staff could apply full `balance_rappen` including frozen pending. Current endpoint delegates to `applyCreditToPayment` → RPC `apply_credit_to_payment`, which computes available as `balance - pending` and debits with `balance - pending >= v_use` under `FOR UPDATE`.

**Evidence:**
- `server/api/credit/use-for-appointment.post.ts:10,127` — calls `applyCreditToPayment(...)`.
- `server/utils/apply-credit-to-payment.ts` — RPC wrapper.
- `migrations/20260915_apply_credit_to_payment.sql` — freeze-aware atomic debit.

---

### 7. High — Concurrent `/payments/process` credit double-spend across payments

**Status: CONFIRMED**

**Attack story:** Per-payment lock (`pending` → `processing`) stops double process of one payment, but two concurrent processes on **different** pending payments both read the same `balance_rappen`, both deduct up to that balance via plain updates → overspend beyond available wallet (and beyond freeze if finding 2 also applies).

**Evidence:**
- `server/api/payments/process.post.ts` — payment claim lock is per `payment.id`; credit path (`:409`, `:524`) is still non-atomic balance RMW, not `deduct_student_credit`.

---

## NEW findings

**None confirmed** (medium/high/critical) beyond the open set above.

Notes (not elevated this pass):
- Historical RLS `student_credits` self-UPDATE was flagged and **dropped** in `migrations/fix_rls_security_audit_march2026.sql` (KRITISCH-04). Live DB policy set not re-verified from this environment.
- `admin/complete-withdrawal` allows role `staff` (message says “Admins”); tenant-scoped; trusted-insider by design.
- Recovery cron amount-gates course fulfillments only; amplifies finding 1 for non-course, not a separate bug.

---

## Cleared / still OK (brief)

- Wallee webhook: API verify + remaining-amount mismatch reject (`webhook.post.ts`, `wallee-remaining-amount.ts`).
- Create-transaction **charge** amount: server uses DB remaining payable (client amount not authoritative).
- `process-public`: server recomputes course amount; rejects client mismatch.
- Top-up credit integrity (`topup-credit.ts` / `apply_wallee_topup_deposit`).
- Course credit enroll: freeze via available wallet + atomic enroll RPC.
- `apply-student-credit.ts` / `student-credits/use-for-appointment`: respect pending freeze.
- `process-withdrawal-wallee`: still 410 disabled.
- Stripe webhook: `constructEvent` signature verify; checkout uses Price IDs.
- Payments PostgREST write lock-down (`20260917_payments_jwt_postgrest_slice_a.sql`).
- Finding 6 fixed via `apply_credit_to_payment`.

---

## Suggested fix order (no code this run)

1. **Finding 1:** Require Wallee API read + `shouldRejectWalleeCaptureMismatch` + merchantReference/payment bind on first-attach and all non-course complete/reuse paths; unique `(wallee_space_id, wallee_transaction_id)` on `payments` where not null.
2. **Findings 2 + 7:** Route all process credit deducts through `deduct_student_credit` (available = balance − pending, atomic).
3. **Findings 3 + 5:** Affiliate bank payout via same atomic deduct (or available-balance CAS).
4. **Finding 4:** `request-credit-withdrawal` → `add_pending_withdrawal` RPC.

---

## docs/bot-reports

- Directory **did not exist** on current `main` @ `e32c5acb` (prior geld reports lived on audit PRs, e.g. `docs/bot-reports/2026-09-17-geld.md`, not merged into main).
- This file: `docs/bot-reports/2026-09-24-geld.md`.
