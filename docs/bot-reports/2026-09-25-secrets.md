# Secrets / Config Check — 2026-09-25

**Scope:** Hardcoded keys in app code, `.env` in diffs/PRs, missing Internal-Secret / cron-auth headers on server→server calls  
**Baseline:** `origin/main` @ `3972f4ce`  
**Result:** 1 bestätigtes Finding

## Checks performed

1. **Keys in code** — Pattern scan for Stripe (`sk_`/`rk_`/`whsec_`), AWS `AKIA`, GitHub `ghp_`, PEM private keys, JWT-like literals in tracked non-example / non-test sources. No live secret values found. Test mocks and docs placeholders remain intentional. `push.ts` PEM markers are format/regex helpers only.
2. **`.env` in diffs** — No non-example `.env` tracked (`.gitignore` covers `.env*`). Only `apps/simy/.env.example` and `apps/website/.env.example` are tracked. Open feature/deps/hotfix/security PR diffs checked (#288, #287, #284, #283, #282, #280, #279, #276, #275, #271, #269, #268, #267, #254, #251, #249, #240, #238, #201, #169, #160): no `.env` additions and no embedded live keys.
3. **Internal-Secrets / cron auth on server calls** — Callers of protected endpoints (`affiliate/process-reward`, email/withdrawal/appointment/course confirmation, `admin/notify-new-tenant`, `appointments/handle-cancellation`, `wallee/save-payment-token`, `emails/send-booking-proposal`, website→simy `upload-inquiry-conversion`, debug→`wallee/webhook`, debug→`process-recalc-queue`, debug→`process-outbound-messages`) pass `x-internal-secret` / `X-Internal-Api-Secret`, `internalSecretHeaders()`, or `Authorization: Bearer $CRON_SECRET` as required. Soft-skip when secret unset remains intentional.

## Findings

### F-01 — Debug calendar sync sends wrong cron auth header

- **Caller:** `server/api/debug/sync-calendars.post.ts` (outbound `$fetch` to cron)
- **Callee:** `server/api/cron/sync-external-calendars.post.ts` via `assertCronRequest()` (`server/utils/cron-auth.ts`)
- **Issue:** Caller forwards `CRON_SECRET` as `x-api-key`. Callee accepts only `Authorization: Bearer $CRON_SECRET`. Effective missing cron credential on the S2S call → always 401.
- **Note:** Debug entrypoint itself correctly requires Bearer; only the nested S2S hop is misconfigured. No secret value exposed in repo.
- **Action:** Out of scope for this bot (report only; no code change).
