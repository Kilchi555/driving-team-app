# Google-Ads-Kontrolle – 2026-08-31

## Ergebnis

**Keine neuen Findings (nicht abschliessend verifiziert).**

Der angeforderte Prüfzeitraum war **2026-08-25 bis 2026-08-31** für:

- `Fahrschule Zürich Umgebung`
- `Fahrschule Lachen Umgebung`

## Suchbegriffe und Negative Keywords

Die Supabase-MCP-Verbindung für das Projekt „Driving Team App“ war in diesem Lauf nicht verfügbar. Ein ausschliesslich lesender Ersatzversuch über die öffentliche PostgREST-Schnittstelle wurde durch die Tabellenberechtigungen mit `401 / permission denied` abgewiesen. Deshalb konnten keine Suchbegriffe aus `marketing_google_ads_search_terms_daily` ausgewertet und keine neuen Kandidaten belastbar identifiziert werden.

Auch die Google-Ads-OAuth-Zugangsdaten waren in der Laufzeitumgebung nicht konfiguriert. Die vorhandenen kampagnenweiten Negativ-Keywords aus `campaign_criterion` mit `negative = true` konnten daher nicht live abgeglichen werden.

## Cannibalization-Check

Der Status der Broad Keywords `fahrschule` und `fahrschule in der nähe` konnte nicht live verifiziert werden. Damit ist offen, ob sie weiterhin genau einmal pro Kampagne aktiviert sind:

- Zürich: `Fahrschule Altstetten`
- Lachen: `Fahrschule Lachen`

Neue aktivierte Duplikate in anderen Anzeigengruppen können ohne die Google-Ads-Leseabfrage nicht ausgeschlossen werden.

## Sicherheit

Es wurde kein Google-Ads-Mutate-Endpunkt aufgerufen und keine Live-Kampagneneinstellung geändert. Dieser Bericht enthält keine Negativ-Keyword-Empfehlungen, weil die erforderlichen Quelldaten und der Live-Abgleich nicht verfügbar waren.
