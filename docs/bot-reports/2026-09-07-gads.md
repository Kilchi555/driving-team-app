# Google-Ads-Kontrolle – 2026-09-07

## Ergebnis

**Keine neuen Findings (Datenprüfung nicht ausführbar).**

Der angeforderte Prüfzeitraum war **2026-09-01 bis 2026-09-07** für:

- `Fahrschule Zürich Umgebung`
- `Fahrschule Lachen Umgebung`

## Suchbegriffe und Negative Keywords

Die Supabase-MCP-Verbindung für das Projekt „Driving Team App“ war in diesem Lauf nicht registriert. Deshalb konnten die Suchbegriffe aus `marketing_google_ads_search_terms_daily` nicht abgefragt und keine neuen irrelevanten Suchbegriffe belastbar identifiziert werden.

Die Google-Ads-OAuth-Zugangsdaten waren in der Laufzeitumgebung ebenfalls nicht konfiguriert. Der erforderliche Live-Abgleich mit den kampagnenweiten Negativ-Keywords aus `campaign_criterion` (`negative = true`) war daher nicht möglich. Es werden keine Negativ-Keyword-Kandidaten empfohlen, solange weder die Suchbegriffe noch ihre bestehende Abdeckung verifiziert werden können.

## Cannibalization-Check

Der Live-Status der Broad Keywords `fahrschule` und `fahrschule in der nähe` konnte nicht geprüft werden. Damit bleibt unbestätigt, ob beide Keywords weiterhin genau einmal pro Kampagne aktiviert und überall sonst pausiert sind:

- Zürich: aktiviert nur in `Fahrschule Altstetten`
- Lachen: aktiviert nur in `Fahrschule Lachen`

Neu aktivierte Duplikate in anderen Anzeigengruppen können ohne die Google-Ads-Leseabfrage nicht ausgeschlossen werden.

## Sicherheit

Es wurde kein Google-Ads-Mutate-Endpunkt aufgerufen und keine Live-Kampagneneinstellung geändert. Der Lauf hat ausschliesslich den verlangten Bericht erzeugt; App- und Datenbankcode blieben unverändert.
