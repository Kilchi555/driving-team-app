# Secrets / Config Check — 2026-09-18

**Scope:** Hardcoded keys in app code, `.env` in diffs/PRs, missing Internal-Secret headers on server→server calls  
**Baseline:** `origin/main` @ `c78d6559`  
**Result:** Keine neuen Findings

## Checks performed

1. **Keys in code** — Pattern scan for Stripe (`sk_`/`rk_`/`whsec_`), Supabase (`sb_secret_`, service-role JWTs), AWS `AKIA`, GitHub `ghp_`, Google `AIza`, PEM private keys in tracked non-example sources. No live secret values found. Delta since last scan (`2caf1857`→`c78d6559`) has no secret-pattern hits. Test mocks remain intentional fixtures only. `push.ts` PEM markers are format/regex helpers, not embedded keys.
2. **`.env` in diffs** — No non-example `.env` tracked in git (`.gitignore` covers `.env*`). Open feature/deps/hotfix PRs checked (#233, #232, #228, #217, #212, #204, #201, #169, #167, #182, #180, #179, #174): no `.env` additions and no embedded live keys in diffs. Only `apps/simy/.env.example` and `apps/website/.env.example` are tracked.
3. **Internal-Secrets on server calls** — Callers of protected endpoints (`affiliate/process-reward`, `email/send-withdrawal-notification`, `admin/notify-new-tenant`, `appointments/handle-cancellation`, `email/send-wallee-payment-link`, `email/send-appointment-notification`, `reminders/send-payment-confirmation`, `wallee/save-payment-token`, `emails/send-booking-proposal`, `emails/send-course-enrollment-confirmation`, `availability/queue-recalc`, website→simy `upload-inquiry-conversion`, debug→`wallee/webhook`) pass `x-internal-secret` / `X-Internal-Api-Secret` or `internalSecretHeaders()` (or staff session where `requireStaffOrInternal` applies). New fulfillment path `sendCourseFulfillmentConfirmation` uses `internalSecretHeaders()`. No missing-header server→server call sites found.

## Findings

Keine neuen Findings.
