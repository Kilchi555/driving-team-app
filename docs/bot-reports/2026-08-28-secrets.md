# Secrets / Config Check — 2026-08-28

**Scope:** Hardcoded keys in app code, `.env` in diffs/PRs, missing Internal-Secret headers on server→server calls  
**Baseline:** `origin/main` @ `2bba3b99`  
**Result:** Keine neuen Findings

## Checks performed

1. **Keys in code** — Pattern scan for Stripe (`sk_`/`rk_`/`whsec_`), Supabase (`sb_secret_`, service-role JWTs), AWS `AKIA`, GitHub `ghp_`, PEM private keys in tracked non-example sources. No live secret values found. Documented public client keys (Firebase `GoogleService-Info.plist`, anon/Maps examples in analysis docs) and truncated JWT samples in guides are out of scope. Diff since last scan (`8b44092a`→`2bba3b99`) introduced no literal secret-like strings.
2. **`.env` in diffs** — No non-example `.env` tracked in git. Open feature/deps PRs checked (#81, #80, #79, #73, #53, #48, #45, #40, #36–#32): no `.env` additions and no embedded live keys in diffs. `.env.example` files contain placeholders / public Place IDs only.
3. **Internal-Secrets on server calls** — Callers of protected endpoints (`affiliate/process-reward`, `email/send-withdrawal-notification`, `admin/notify-new-tenant`, `appointments/handle-cancellation`, `email/send-wallee-payment-link`, `email/send-appointment-notification`, `reminders/send-payment-confirmation`, `wallee/save-payment-token`, `emails/send-booking-proposal`, website→simy `upload-inquiry-conversion`) all pass `x-internal-secret` / `X-Internal-Api-Secret` or `internalSecretHeaders()`.

## Findings

Keine neuen Findings.
