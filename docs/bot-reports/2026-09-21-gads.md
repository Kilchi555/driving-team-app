# Google-Ads-Report vom 2026-09-21

## Ergebnis

Keine neuen Findings.

Für den Prüfzeitraum 2026-09-15 bis 2026-09-21 (UTC) konnten keine belastbaren
neuen Negative-Keyword-Kandidaten ermittelt werden. Das ist kein bestätigter
Clean-Befund: Die benötigten Live-Daten waren in diesem Lauf nicht verfügbar.

## Datenzugriff

- Das Supabase MCP für das Projekt „Driving Team App“ war in diesem Lauf nicht
  registriert. Ein zusätzlicher, rein lesender PostgREST-Versuch erreichte das
  Projekt, wurde aber durch RLS mit `401 permission denied` für
  `marketing_google_ads_search_terms_daily` abgewiesen.
- Die Google-Ads-OAuth-Konfiguration war nicht vorhanden. Deshalb konnten die
  Kampagnen-Negatives aus `campaign_criterion` nicht gelesen und Suchbegriffe
  nicht gegen den Live-Bestand abgeglichen werden.
- Es wurde kein Google-Ads-Mutate-Endpunkt aufgerufen und keine Live-Einstellung
  verändert.

## Negative Keywords

Mangels abrufbarer Suchbegriffe und Live-Negatives gibt es keine verifizierten
neuen Kandidaten. Insbesondere darf dieses Ergebnis nicht als Nachweis gewertet
werden, dass Motorrad-, VKU-, WAB-, Nothelfer-, Lastwagen-, Anhänger-,
Mitbewerber-, Gebiets- oder fremdsprachige Suchanfragen vollständig abgedeckt
sind.

Die bestehenden Listen `EXTRA_NEGATIVES` und `ZUERICH_AUTO_NEGATIVES` wurden
gemäss dem Report-only-Auftrag nicht verändert.

## Catch-all-Kontrolle

Der aktuelle Status der Broad Keywords `fahrschule` und
`fahrschule in der nähe` konnte nicht live geprüft werden. Der Sollzustand
bleibt:

- `Fahrschule Zürich Umgebung`: beide Keywords genau einmal `ENABLED` in
  `Fahrschule Altstetten`, sonst überall `PAUSED`.
- `Fahrschule Lachen Umgebung`: beide Keywords genau einmal `ENABLED` in
  `Fahrschule Lachen`, sonst überall `PAUSED`.

Neue aktivierte Duplikate können ohne Google-Ads-Lesezugriff weder bestätigt
noch ausgeschlossen werden.
