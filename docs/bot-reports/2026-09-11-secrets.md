# Secrets / Config Check — 2026-09-11

**Scope:** Hardcoded keys in app code, `.env` in diffs/PRs, missing Internal-Secret headers on server→server calls  
**Baseline:** `origin/main` @ `2caf1857`  
**Result:** Keine neuen Findings

## Checks performed

1. **Keys in code** — Pattern scan for Stripe (`sk_`/`rk_`/`whsec_`), Supabase (`sb_secret_`, service-role JWTs), AWS `AKIA`, GitHub `ghp_`, Google `AIza`, PEM private keys in tracked non-example sources. No live secret values found. Delta since last scan (`571e2e8f`→`2caf1857`) only added intentional test mocks (`sk_test_mock_not_live`, `sk_live_should_not_leak` / leak-assertion fixtures). `push.ts` PEM markers are format/regex helpers, not embedded keys.
2. **`.env` in diffs** — No non-example `.env` tracked in git (`.gitignore` covers `.env*`). Open feature/deps/hotfix PRs checked (#182, #180, #179, #174, #169, #167, #166, #160, #148, #143, #141, #128): no `.env` additions and no embedded live keys in diffs. Only `apps/simy/.env.example` and `apps/website/.env.example` are tracked.
3. **Internal-Secrets on server calls** — Callers of protected endpoints (`affiliate/process-reward`, `email/send-withdrawal-notification`, `admin/notify-new-tenant`, `appointments/handle-cancellation`, `email/send-wallee-payment-link`, `email/send-appointment-notification`, `reminders/send-payment-confirmation`, `wallee/save-payment-token`, `emails/send-booking-proposal`, `emails/send-course-enrollment-confirmation`, `availability/queue-recalc`, website→simy `upload-inquiry-conversion`, debug→`wallee/webhook`) all pass `x-internal-secret` / `X-Internal-Api-Secret` or `internalSecretHeaders()`. No missing-header call sites found.

## Findings

Keine neuen Findings.
