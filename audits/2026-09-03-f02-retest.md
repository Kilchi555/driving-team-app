# F-02 Retest — Public `/api/auth/manage` Auth Bypass

**Date:** 2026-09-03  
**Scope:** Live verification that F-02 is closed in production after #135  
**Production:** `https://app.simy.ch`  
**Remediation merge:** `fd8683f3` — `fix(auth): close F-02 public auth management bypass (#135)`  
**Status:** **CLOSED**

---

## Method

Unauthenticated `POST /api/auth/manage` against production with former bypass actions. No service-role credentials used. No Auth users created.

---

## Live results (2026-09-03)

| Test | Request | HTTP | Body |
|------|---------|------|------|
| **A** `signin-password` | `{"action":"signin-password","email":"nobody@example.com","password":"x"}` | **410** | `data.code = AUTH_MANAGE_RETIRED`, `action: signin-password` |
| **B** `signup` | `{"action":"signup","email":"f02-retest-<ts>@example.com","password":"Test1234!"}` | **410** | `data.code = AUTH_MANAGE_RETIRED`, `action: signup` |

Both responses include the retired action list (`signin-password`, `signup`, `reset-password-email`, `get-session`, `set-session`, `update-user`) and do **not** return a session or create an Auth user.

---

## Comparison to pre-remediation verification (#132)

| Before (#132 draft) | After (#135 deploy) |
|---------------------|---------------------|
| Public `signup` created confirmed Auth users via service role | **410** — no Auth path |
| Public `signin-password` returned a full session (bypassed login controls) | **410** — no session |
| Production blocker: YES | Production blocker: **NO** for F-02 |

Draft PR #132 (`docs(security): F-02 verification — still vulnerable`) is **superseded** and should be closed without merge.

---

## Follow-up (invite UX, not F-02 bypass)

Post-merge Bugbot notes on invite password set (`pages/login/set-password.vue`):

1. PKCE double-exchange soft-fail when `@nuxtjs/supabase` already established the session.
2. Clear hash/query secrets from the address bar after `setSession` / exchange.

Those are handled in the F-02 follow-up hotfix (same ship window as this retest note). They do **not** reopen the public manage bypass.

---

## Verdict

**F-02: CLOSED in production**  
**Production Blocker: NO** (for this finding)
